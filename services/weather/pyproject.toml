[project]
name = "weather"
version = "0.1.0"
description = "Weather service"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.119.0",
    "redis>=5.2.1",
    "shared-py",
]

[tool.uv.sources]
shared-py = { path = "../shared-py" }